import { GtagOutboundClickEventDirective } from "./gtag-outbound-click-event.directive";

describe("EventPreventDefaultDirective", () => {
  it("should create an instance", () => {
    const directive = new GtagOutboundClickEventDirective();
    expect(directive).toBeTruthy();
  });
});
